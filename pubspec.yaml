name: behavior_change_app
description: "HealthFlexx Coach — AI-powered health behavior change"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.8.0
  http: ^1.2.0
  flutter_secure_storage: ^9.2.0
  provider: ^6.1.0
  workmanager: ^0.9.0
  flutter_local_notifications: ^18.0.1
  firebase_core: ^3.12.0
  firebase_messaging: ^15.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
